import 'package:flutter/material.dart';

class TestHive extends StatelessWidget {
  const TestHive({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('test_hive'),
        backgroundColor: Colors.blue,
      ),
    );
  }
}
